<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://catanzaromj.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://catanzaromj.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-03-16T11:58:44+00:00</updated><id>https://catanzaromj.github.io/feed.xml</id><title type="html">Michael J. Catanzaro</title><subtitle>Machine learning, topological data analysis, and software development. Research and engineering at the intersection of algebraic topology and AI. </subtitle><entry><title type="html">Tabular Diffusion in Practice: SDEdit and Conditioning</title><link href="https://catanzaromj.github.io/blog/2025/tabular-diffusion3/" rel="alternate" type="text/html" title="Tabular Diffusion in Practice: SDEdit and Conditioning"/><published>2025-12-18T00:00:00+00:00</published><updated>2025-12-18T00:00:00+00:00</updated><id>https://catanzaromj.github.io/blog/2025/tabular-diffusion3</id><content type="html" xml:base="https://catanzaromj.github.io/blog/2025/tabular-diffusion3/"><![CDATA[<p>This post continues from <a href="/blog/2025/tabular-diffusion2/">part two</a>, which covered variance schedules, time embeddings, EMA, and classifier guidance. Here I want to focus on two techniques that let you get more out of a model you have already trained: SDEdit for targeted sample modification, and conditioning for steering generation toward specific properties.</p> <h2 id="sdedit-for-targeted-sample-modification">SDEdit for targeted sample modification</h2> <p>Once you have a trained diffusion model, generating samples from pure noise is only one option. Given an existing valid sample, can we produce a nearby sample with slightly different properties? This is exactly what <a href="https://arxiv.org/abs/2108.01073">SDEdit</a> (Meng et al., 2021) enables.</p> <p>The idea is straightforward. Rather than starting from \(x_T \sim \mathcal{N}(0, I)\), take an existing sample \(x_0\), add noise up to some intermediate timestep \(t^* &lt; T\), and run the reverse process from there. Formally,</p> \[x_{t^*} = \sqrt{\bar{\alpha}_{t^*}}\, x_0 + \sqrt{1 - \bar{\alpha}_{t^*}}\, \epsilon, \qquad \epsilon \sim \mathcal{N}(0, I)\] <p>then denoise from \(t^*\) back to \(0\) as usual. The choice of \(t^*\) controls the fidelity-diversity tradeoff: a small \(t^*\) produces outputs close to the original, while a large \(t^*\) allows the model more freedom to deviate.</p> <p>For our tabular setting this is particularly useful. We have a library of known-valid samples from the training set. Using SDEdit we can take one of these, noise it slightly, and denoise it back, producing a new sample that respects the overall data distribution without starting from scratch.</p> <h3 id="combining-sdedit-with-auxiliary-guidance">Combining SDEdit with auxiliary guidance</h3> <p>The more interesting case is using SDEdit together with our auxiliary guidance functions. During the reverse process from \(t^*\), we apply the same guided update as in standard sampling:</p> \[\tilde{\mu}_\theta(x_t, t) = \mu_\theta(x_t, t) + s \cdot \Sigma_\theta(x_t, t) \nabla_{x_t} \log p(y \mid x_t) - \sum_i \lambda_i \nabla_{x_t} f_{\phi_i}(x_t)\] <p>By tuning the \(\lambda_i\) during the SDEdit reverse pass, we can steer the output toward specific auxiliary properties. We can try to nudge a sample to have a higher value of a particular predicted quantity, while keeping it grounded in a valid starting point. This gives a practical tool for targeted design: instead of generating a thousand random samples and filtering, you can take a known-good sample and walk it toward a desired region of the data manifold.</p> <p>One subtlety we found important is that applying constant \(\lambda_i\) throughout the entire reverse pass tends to over-steer the sample. The guidance signal is strongest and most meaningful at intermediate timesteps, where the sample has enough structure for the auxiliary network gradients to be informative. Near \(t = 0\), the sample is nearly clean and small gradient nudges can introduce artifacts or push it outside the valid region.</p> <p>To address this, we scheduled the \(\lambda_i\) to decay toward zero as \(t \to 0\). A simple linear decay works well:</p> \[\lambda_i(t) = \lambda_i \cdot \frac{t}{t^*}\] <p>so that guidance is at full strength at the start of the reverse pass and switched off entirely at the final step. This lets the model’s own learned denoising close out the sample cleanly, without the auxiliary functions fighting against it at the last moment.</p> <h2 id="conditioning">Conditioning</h2> <p>Classifier guidance (covered in <a href="/blog/2025/tabular-diffusion2/">part two</a>) steers sampling at inference time using the gradient of an external classifier. Conditioning instead bakes the desired properties directly into the diffusion model during training, so that the model learns to generate samples that respect them from the start.</p> <p>Our conditioning signal consisted of 6 scalar labels. These were a mix of guidance function values and input parameters that we wanted to be able to specify at generation time. To handle the case where only some labels are known, we augmented the condition vector with a binary mask of the same length. The full conditioning input was therefore a vector of dimension 12:</p> \[c = [v_1, \ldots, v_6,\; m_1, \ldots, m_6], \qquad m_i \in \{0, 1\}\] <p>where \(m_i = 1\) indicates that label \(v_i\) is active and should be respected, and \(m_i = 0\) indicates that no constraint is placed on that dimension. During training, we randomly zeroed out subsets of the mask, which taught the model to generate valid samples whether a full set of conditions, a partial set, or no conditions at all were provided. This is the core idea behind classifier-free guidance, but applied per-label rather than all-or-nothing.</p> <p>The conditioning vector \(c\) was injected into the denoising network alongside the time embedding, so at each reverse step the model sees both where it is in the diffusion process and what properties the final sample should have.</p> <p>At inference, you specify whichever labels matter for a given run and set the corresponding mask entries to 1, leaving the rest at 0. The model generates freely along unconstrained dimensions while respecting the active labels. This is particularly useful in our setting because the 6 quantities of interest were not always all specified. Only 2 or 3 labels were typically relevant for a given design goal.</p> <h2 id="takeaways">Takeaways</h2> <p>SDEdit and conditioning solve related but distinct problems. One way to think about them is as follows. SDEdit is about the starting point of the diffusion process. Rather than generating from pure noise, you begin from a known-good sample and let the reverse process explore nearby. Conditioning is about the endpoint constraints. Conditioning allows the model to learn to generate samples that satisfy specified properties regardless of where it starts.</p> <p>Used together, they are complementary. You can take a known-valid sample, noise it to \(t^*\), and run a conditioned reverse pass. This grounds the output in a realistic starting point while steering it toward a desired region of the design space.</p> <p>A few things worth carrying forward:</p> <ul> <li>Randomly dropping label subsets during training means one model covers all possible partially-specified queries at inference. Separate models for each query pattern would be impractical.</li> <li>Whether using SDEdit or standard sampling with auxiliary functions, decaying \(\lambda_i\) toward zero near \(t = 0\) consistently improved output quality. Guidance is most useful when the sample is still rough; let the model finish on its own terms.</li> <li>During conditioned generation you can still apply auxiliary guidance functions on top. The conditioning shapes the overall distribution; the guidance nudges specific properties within it. They operate at different levels and do not cancel each other out.</li> </ul>]]></content><author><name></name></author><category term="ml"/><category term="diffusion-models"/><category term="generative-models"/><category term="tabular-data"/><category term="machine-learning"/><summary type="html"><![CDATA[Two techniques for steering a trained diffusion model toward specific outputs without retraining it.]]></summary></entry><entry><title type="html">Tabular Diffusion in Practice: Variance Schedules, Time Embeddings, and EMA</title><link href="https://catanzaromj.github.io/blog/2025/tabular-diffusion2/" rel="alternate" type="text/html" title="Tabular Diffusion in Practice: Variance Schedules, Time Embeddings, and EMA"/><published>2025-09-13T00:00:00+00:00</published><updated>2025-09-13T00:00:00+00:00</updated><id>https://catanzaromj.github.io/blog/2025/tabular-diffusion2</id><content type="html" xml:base="https://catanzaromj.github.io/blog/2025/tabular-diffusion2/"><![CDATA[<p>This post assumes familiarity with the basics of denoising diffusion. If you want a primer first, see <a href="/blog/2025/tabular-diffusion/">my earlier post</a>.</p> <p>Here I want to get into the details that I found matter in practice. These include how you schedule noise, how the network knows where it is in the diffusion process, how you stabilize training, and how you steer samples toward valid outputs at inference time. Most of what I write here are things that I found out through experience, not necessarily in literature.</p> <h2 id="our-project">Our project</h2> <p>The project we were working on consisted of roughly 50,000 data points of tabular data. Each data point consisted of 21 numerical values, some unitless and some with units on very different scales. There were roughly 30 equations which constrained the values in each row. So while the first column may have had a range of possible values of say \(-10\) to \(10\), there were additional constraints on it involving the other columns. While our intention was not to have the de-noising model learn all of these relationships explicitly, it tended to do so in practice.</p> <h2 id="variance-schedules">Variance schedules</h2> <p>The variance schedule \(\{\beta_t\}\) determines how noise accumulates over the forward process. I found this choice to have a huge impact on how much signal remains at intermediate timesteps and how hard the reverse process is to learn.</p> <p>We’ll get to the individual variance schedules below. But regardless of which form of \(\beta_t\) you choose, an important quantity to track is the <em>cumulative product</em> \(\bar{\alpha}(t) = \prod_{s=1}^{t}(1 - \beta_s)\).</p> <p>With this quantity, you can re-write the forward process in closed form directly from \(x_0\):</p> \[q(x_t \mid x_0) = \mathcal{N}(x_t;\sqrt{\bar{\alpha}_t}x_0, (1 - \bar{\alpha}_t) I)\] <p>Now the variance term with \((1 - \bar{\alpha}_t)I\) makes explicit the signal to noise ratio at each time step. We see that \(\bar{\alpha}_t\) is essentially the signal retention over time. Plotting this as a function of \(t\) can help us tweak and understand the diffusion model’s performance.</p> <p>We considered linear, quadratic, and cosine based schedules for variance. A linear schedule looks something like</p> \[\beta_t = \beta_{\min} + \frac{t-1}{T-1}(\beta_{\max} - \beta_{\min})\] <p>with typical values \(\beta_{\min} = 10^{-4}\), \(\beta_{\max} = 0.02\), and \(T = 1000\). You can imagine what a quadratic schedule looks like based on this.</p> <p>A cosine schedule typically takes the form</p> \[\bar{\alpha}_t = \frac{f(t)}{f(0)}, \qquad f(t) = \cos\left(\frac{t/T + s}{1 + s} \cdot \frac{\pi}{2}\right)^2\] <p>where \(s \approx 0.008\) is a small offset to keep \(\beta_t\) from being too small near \(t = 0\). Then \(\beta_t = 1 - \bar{\alpha}_t / \bar{\alpha}_{t-1}\).</p> <p>Plotting \(\bar{\alpha}_t\) makes the differences between these three cases concrete:</p> <div id="alpha-bar-plot" style="width:100%; height:400px;"></div> <script>!function(){function t(){function t(t){const e=[];let n=1;for(const o of t)n*=1-o,e.push(n);return e}const e=1e3,n=1e-4,o=.02,a=.008,i=Array.from({length:e},(t,e)=>e+1),l=i.map(t=>n+(t-1)/(e-1)*(o-n)),s=i.map(t=>n+Math.pow((t-1)/(e-1),2)*(o-n)),r=Math.pow(Math.cos(a/(1+a)*Math.PI/2),2),p=i.map(t=>Math.pow(Math.cos((t/e+a)/(1+a)*Math.PI/2),2)/r),c=[{x:i,y:t(l),name:"Linear",mode:"lines"},{x:i,y:t(s),name:"Quadratic",mode:"lines"},{x:i,y:p,name:"Cosine",mode:"lines"}],m={xaxis:{title:"t"},yaxis:{title:"\u1fb1_t (signal retention)",range:[0,1]},legend:{orientation:"h",y:-.2},margin:{t:20}};Plotly.newPlot("alpha-bar-plot",c,m,{responsive:!0})}if(window.Plotly)t();else{var e=document.createElement("script");e.src="https://cdn.plot.ly/plotly-3.4.0.min.js",e.onload=t,document.head.appendChild(e)}}();</script> <p>The cosine schedule keeps this higher for longer before dropping off, which is why it tends to produce better samples.</p> <p>Our choice was motivated by our results — we tried lots of different variance schedules and found linear worked best. We believe it was in part because we also quantile normalized our data. This puts all the features on similar scales with similar distributions. We determined “best” by lots of trial and error, and validating the outputs. Because our tabular data was pretty constrained, it is very easy to determine what percentage of outputs are actually “valid” in a concrete way.</p> <h2 id="time-embeddings">Time embeddings</h2> <p>The network needs to know which timestep \(t\) it is operating at. In particular, the noise behavior changes as a function of \(t\) and so the network needs to have some internal state of this. Denoising at \(t=1\) with tiny noise is very different than at \(t=1000\) which is pure noise. The model needs to be able to adapt its denoising behavior to how noisy the input is. This seems like a trivial task–just give it the time parameter \(t\) with the current noised data point \(x_t\). But neural networks work with respect to arrays and tensors, not integers. So instead we embed time into the network via a sinusoidal embedding, analogous to positional encodings in transformers.</p> <p>First, we define the fundamental “frequencies”</p> \[\omega_k = \exp!\left(-\frac{k \ln(P)}{\lfloor d/2 \rfloor}\right), \qquad k = 0, 1, \ldots, \lfloor d/2 \rfloor - 1\] <p>Then we can define the formal embedding by concatenating cosine and sine halves</p> \[\text{emb}(t) = \bigl[\cos(a_{t,0}),\ \ldots,\ \cos(a_{t,\lfloor d/2\rfloor-1}),\ \sin(a_{t,0}),\ \ldots,\ \sin(a_{t,\lfloor d/2\rfloor-1})\bigr]\] <p>where the arguments are defined as (outer product of timesteps and frequencies):</p> \[a_{t,k} = t \cdot \omega_k\] <p>where \(d = 128\), the embedding dimension and \(P\) is the maximum period. Note that our implementation puts cosine first, then sine, which is the opposite of the transformer convention, but functionally equivalent.</p> <p>The embedding is projected and injected into the network with a residual connection to inject this information early on and again at the end. Our network was a</p> <h2 id="exponential-moving-average">Exponential moving average</h2> <p>During training, model weights are updated via gradient descent as usual. Exponential moving average (EMA) maintains a separate shadow copy of the weights as a running average:</p> \[\theta_{\text{ema}} \leftarrow \mu \cdot \theta_{\text{ema}} + (1 - \mu) \cdot \theta\] <p>where \(\mu\) is the decay rate (typically 0.999 or 0.9999). The EMA weights are not used during training — only at inference.</p> <p>The intuition is that the EMA weights average out gradient noise and tend to sit in flatter regions of the loss landscape. For tabular data, where training sets can be small relative to image benchmarks, this stabilization matters more than you might expect.</p> <p>We found a pretty dramatic difference between EMA and non-EMA samples. EMA samples tended to be valid almost 30% more of the time compared to the non-EMA samples. They also tended to be more realistic, with the non-EMA samples being more wild.</p> <h2 id="guidance-at-inference">Guidance at inference</h2> <p>A guidance function steers the denoising trajectory toward outputs that satisfy some external criterion — without retraining the diffusion model. At each reverse step, the gradient of a guidance signal with respect to \(x_t\) nudges the predicted mean:</p> \[\tilde{\mu}_\theta(x_t, t) = \mu_\theta(x_t, t) + s \cdot \Sigma_\theta(x_t, t) \nabla_{x_t} \log p(y \mid x_t)\] <p>where \(s\) is a guidance scale and \(p(y \mid x_t)\) scores how plausible or valid a noisy sample is. In practice, we let \(s\) be near 0.4, but of course this depends on the magnitude of the gradient. This parameter was adjusted several times throughout, especially after re-training (see below).</p> <p>In our set-up, we had a variety of guidance functions to choose from. First, we had a trained classifier that predicted if a sample was valid or not (exactly the \(p\) described above). But we could also augment the above formula with other guidance functions. In addition, we trained over 30 neural networks to predict auxiliary quantities of interest for each produced sample, trained on the initial training set. With these additional neural networks, the mean for the sampling process becomes</p> \[\tilde{\mu}_\theta(x_t, t) = \mu_\theta(x_t, t) + s \cdot \Sigma_\theta(x_t, t) \nabla_{x_t} \log p(y \mid x_t) - \sum_i \lambda_i \nabla_{x_t} f_{\phi_i}(x_t)\] <p>where \(f_{\phi_i}\) are the auxiliary guidance functions. By tuning the \(\lambda_i\) parameters, we can modify the influence of each specific auxiliary function.</p> <p>Initially, we trained the classifier \(p(y)\) for predicting valid samples to near 100% accuracy on the training set. As a result, the classifier was over confident and its gradient became very unstable. In particular, the gradient term dominated the guided diffusion and biased the samples to one of three local regions with the total sample space. We realized this mistake and re-trained the classifier with:</p> <ul> <li>noisy data: we added noise to our training data during sampling</li> <li>label smoothing: we turned hard targets like <code class="language-plaintext highlighter-rouge">valid</code> and <code class="language-plaintext highlighter-rouge">invalid</code>, codified as <code class="language-plaintext highlighter-rouge">[1,0]</code> and <code class="language-plaintext highlighter-rouge">[0,1]</code>, into soft targets like <code class="language-plaintext highlighter-rouge">[0.9, 0.1]</code></li> <li>increase regularization: we increased the dropout rates and weight decays</li> <li>early stopping: we modified the stopping criteria to be based on calibration and confidence, not accuracy.</li> </ul> <p>With these modifications, the samples generated from the diffusion model were much more realistic and varied. The key lesson here is that guidance quality is as important as diffusion quality. A poorly calibrated classifier can actively harm sample diversity even when the underlying model is strong.</p> <h2 id="takeaways">Takeaways</h2> <p>A few things that surprised us or that I would have liked to know earlier:</p> <ul> <li>Quantile normalization changed the data distribution enough that the cosine schedule’s advantages largely disappeared. Benchmark your schedule choice after deciding on normalization.</li> <li>For tabular data with smaller training sets, the ~30% validity improvement we saw from EMA was not a marginal gain. For us, it was the difference between a usable and an unusable model.</li> <li>Mode collapse disguised as high accuracy is a real failure mode. Train the guidance classifier on noisy inputs, use label smoothing, and evaluate on calibration rather than raw accuracy.</li> <li>The \(\lambda_i\) parameters let you dial in how strongly each auxiliary function influences sampling, which is much more flexible than retraining the diffusion model itself.</li> </ul>]]></content><author><name></name></author><category term="ml"/><category term="diffusion-models"/><category term="generative-models"/><category term="tabular-data"/><category term="machine-learning"/><summary type="html"><![CDATA[A closer look at the implementation choices that matter when training a diffusion model on tabular data.]]></summary></entry><entry><title type="html">Diffusion Models for Tabular Data</title><link href="https://catanzaromj.github.io/blog/2025/tabular-diffusion/" rel="alternate" type="text/html" title="Diffusion Models for Tabular Data"/><published>2025-05-06T00:00:00+00:00</published><updated>2025-05-06T00:00:00+00:00</updated><id>https://catanzaromj.github.io/blog/2025/tabular-diffusion</id><content type="html" xml:base="https://catanzaromj.github.io/blog/2025/tabular-diffusion/"><![CDATA[<p>Diffusion models have become the dominant paradigm for image and audio generation. Extending them to the tabular data of spreadsheets and databases is less straightforward, and the reasons why are worth understanding.</p> <h2 id="what-makes-tabular-data-different">What makes tabular data different</h2> <p>There are a variety of reasons why image and audio data is significantly different than tabular data. Images and audio data tend to be continuous and locally correlated. Nearby pixels tend to have similar values, and the sound of a song tends to build and fall fairly continuously throughout a piece of music. Diffusion models are based on a Gaussian noise process and so these data types tend to fit naturally with this mathematical setup. Unfortunately, tabular data breaks both assumptions.</p> <p>A typical table might have a mix of continuous features (like age, income, temperature), categorical features (like country, product type, diagnosis code), and ordinal (or count features) that live somewhere in between. There is no natural notion of “nearby” across columns, and the marginal distributions of individual columns can be wildly non-Gaussian. They can be heavy-tailed, multimodal, or discrete and of course, all of this can vary from column to column.</p> <h2 id="the-diffusion-framework-briefly">The diffusion framework, briefly</h2> <p>A diffusion model (here we take standard de-noising diffusion) works by defining a forward process that gradually noises initial data \(x_0\) with Gaussian noise over \(T\) steps:</p> \[q(x_t \mid x_{t-1}) = \mathcal{N}(x_t;\, \sqrt{1 - \beta_t}\, x_{t-1},\, \beta_t I)\] <p>The noise serves to corrupt the data over these time steps, turning your training data into pure noise. The <em>variance schedule</em> \(\{\beta_t\} = \beta_0, \beta_1, \beta_2, \ldots, \beta_T\) controls the statistics of the noise as a function of \(t\). Maybe we’ll explore this parameter in a future post but for now, we think of it as the amount of noise added at each step.</p> <p>A diffusion model is a neural network that learns to <strong>reverse</strong> this process. The network learns to predict \(x_{t-1}\) given \(x_t\).</p> \[p_\theta(x_{t-1} \mid x_t) = \mathcal{N}(x_{t-1};\, \mu_\theta(x_t, t),\, \Sigma_\theta(x_t, t))\] <p>where \(\mu_\theta\) and \(\Sigma_\theta\) are the mean and variance predicted by the neural network. Again, maybe we’ll dive into this more in a future post, but for now, we view these as parameters learned by the network to accurately predict the noise.</p> <p>Again, this is worth saying: the network learns to predict the noise added each time step. This is something that is easy to overlook, but important to remember. The network does not learn to generate new samples per se. It learns to de-noise one data point at one time, into another at the next time. During inference, the network generates new instances of data by starting from pure (randomly generated) noise and denoising iteratively using the network. During training, the network learns specific features and characteristics of the data to de-noise to.</p> <p>The key requirement/assumption is that the noising process is tractable and that the data space is continuous and well-suited to Gaussian perturbations. Tabular data satisfies neither cleanly.</p> <h2 id="approaches-for-handling-mixed-types">Approaches for handling mixed types</h2> <p>Several strategies have been explored:</p> <ol> <li> <p>Encode everything as continuous. Categorical variables can be embedded or one-hot encoded before diffusion, then decoded back post-sampling. The risk is that the model learns to sample in a space that doesn’t respect category boundaries. I find this approach to be the least informed, but it does work in practice.</p> </li> <li> <p>Score matching on a mixed space. You could do separate noise processes for continuous and discrete variables and then train a joint score function. This is more principled than option 1 but can be harder to train. There might not be a reason a priori to expect your data to split so nicely between continuous and discrete.</p> </li> <li> <p>Auxiliary networks to guide sampling. Rather than modifying the noise process, one approach would be to add conditioning or a guidance network that steers the denoising trajectory toward valid, coherent outputs. This can be useful when tabular rows must satisfy structural constraints (e.g., clinical plausibility, valid configurations, etc). For the project I was involved with, this was the approach that we took.</p> </li> </ol> <h2 id="why-this-is-hard-to-evaluate">Why this is hard to evaluate</h2> <p>Evaluating generative models for tabular data is an open problem. Even for generative imaging models, it is tough. Image models can be assessed with FID or human judgment, but these may suffer from their own inherent biases or may be too costly in practice. For tabular data, common proxies include:</p> <ul> <li>Train on synthetic, test on real (TSTR). Train a downstream model on generated data, but then evaluate on real held-out data.</li> <li>Statistical fidelity. Compute marginal distributions, pairwise correlations, and higher-order statistics (again) on real vs. synthetic data.</li> <li>Privacy metrics. How easy is it to recover a real training record from a generated one?</li> </ul> <p>None of these alone is sufficient, and they can conflict.</p> <h2 id="my-thoughts">My thoughts</h2> <p>Given the plentiful amounts of tabular data available, I find it surprising that more research isn’t done on this. I know a picture is worth 1000 words, but there are tons of business opportunities for analyzing and re-creating such data. Images and audio often require curation and labeling to be useful for training. Healthcare records, financial transactions, logistics data, etc. are already labeled. It seems like there is a huge supply of interesting tabular data that exceeds what is being studied.</p> <p>This last point also ties in to the privacy metrics point of view. A health clinic cannot release patient data just as a bank can’t release transaction data. A good generative model can help change that by allowing us to study data that can’t be shared. Synthetic tabular data generated from these sources has real and concrete use cases. This feels commercially real to me in a way that image generation is only part of the time.</p> <h2 id="further-reading">Further reading</h2> <p>There are way better references for this material than this post. This is just meant to be my understanding of this at the time of writing.</p> <ul> <li><a href="https://arxiv.org/abs/2006.11239">Denoising Diffusion Probabilistic Models</a> — Ho et al. (2020). The original paper this post is based on.</li> <li><a href="https://arxiv.org/abs/2209.15421">TabDDPM: Modelling Tabular Data with Diffusion Models</a> — Kotelnikov et al. (2023). A direct adaptation of DDPM to tabular data.</li> <li><a href="https://arxiv.org/abs/2011.13456">Score-Based Generative Modeling through Stochastic Differential Equations</a> — Song et al. (2020). The score matching perspective on diffusion.</li> <li><a href="https://arxiv.org/abs/1907.00503">Modeling Tabular Data using Conditional GAN</a> — Xu et al. (2019). A GAN-based baseline; useful for comparisons.</li> </ul>]]></content><author><name></name></author><category term="ml"/><category term="diffusion-models"/><category term="generative-models"/><category term="tabular-data"/><category term="machine-learning"/><summary type="html"><![CDATA[Why tabular data is a hard target for generative models, and how diffusion models are being adapted to handle it.]]></summary></entry><entry><title type="html">Building Intuition for Persistent Homology</title><link href="https://catanzaromj.github.io/blog/2023/persistent-homology/" rel="alternate" type="text/html" title="Building Intuition for Persistent Homology"/><published>2023-03-02T00:00:00+00:00</published><updated>2023-03-02T00:00:00+00:00</updated><id>https://catanzaromj.github.io/blog/2023/persistent-homology</id><content type="html" xml:base="https://catanzaromj.github.io/blog/2023/persistent-homology/"><![CDATA[<p>In this post, I want to explore persistent homology and some intuition surrounding it without leaning too hard on formalism.</p> <h2 id="the-problem-data-has-shape">The problem: data has shape</h2> <p>Imagine you sample a hundred points from a circle. There is no oracle that says “this came from a circle”. Instead you just have a list of \((x, y)\) coordinates. But the shape is there, latent in the point cloud. If you could detect it, you’d know the data lives on a loop, not a blob.</p> <p>Now imagine the same number of points sampled from two separate clusters, or from a torus. The statistics of the datasets are different (e.g. mean, variance, pairwise distances), but the topological fingerprint changes too, and often more clearly. Persistent homology is a way to read that topological fingerprint and analyze the implications of it on your dataset.</p> <h2 id="the-filtration">The Filtration</h2> <p>The key idea is surprisingly simple. Put a ball of radius \(r\) around each point. Start with \(r = 0\) (so that each point is isolated) and slowly increase \(r\).</p> <p>As \(r\) grows, two things happen:</p> <ol> <li>Balls overlap. When two balls first touch, we draw an edge between the corresponding points.</li> <li>Edges form triangles. When three points are all mutually connected, we fill in a triangle.</li> </ol> <p>This growing process is called the Vietoris-Rips <em>filtration</em>. At each value of \(r\), we have a combinatorial object (a simplicial complex) that approximates the shape of the data at that scale.</p> <p>By topological features, we mean connected components, loops, voids, and higher dimensional analogues. As \(r\) increases, these features can appear and disappear. A connected component is born when a point first appears (always at \(r = 0\)) and dies when it merges with another component. A loop is born when a ring of edges closes up, and dies when the interior fills with triangles.</p> <h2 id="persistence-diagrams">Persistence diagrams</h2> <p>For each feature we record two numbers: the radius at which it was <em>born</em> and the radius at which it <em>died</em>. Plot birth on the \(x\)-axis, death on the \(y\)-axis. Altogether, these form a <em>persistence diagram</em>.</p> <p>Every point lies above the diagonal, since death always follows birth. Note though that due to computational artifacts, some software packages may produce points on the diagonal and these should be ignored. The vertical distance from the diagonal is called persistence and is a measure of the scale over which a feature survives.</p> <p>It is tempting to read long-lived features as “signal” and short-lived ones as “noise.” This is often done in many tutorials on persistence, and there are lots of example cases in which this is true. This framing might even come from the stability theorem, which guarantees that small perturbations to the data move points in the diagram by at most the same amount. But stability and significance are different things. Short-lived features are stable to small perturbations; they simply live at a finer scale. In the circle-of-circles example discussed below, the small loops inside each cluster die quickly, yet they are exactly what distinguishes that dataset from a plain circle.</p> <p>Perhaps a better reading is that the diagram gives you the full multi-scale topology of the data. Which features are relevant depends on the scale your application cares about.</p> <h2 id="try-it-out">Try it out</h2> <p>The best way to build intuition is to interact with the filtration directly. The Persistent Homology Explorer tool allows you to mess around with persistence for a variety of small toy datasets. You can pick a datapoint set, drag the radius slider, and watch how features appear in the diagram as \(r\) grows.</p> <div style="margin: 2em 0;"> <a href="/assets/html/persistence_explorer.html" target="_blank" style="font-family: monospace;"> → Open the Persistent Homology Explorer </a> </div> <p>A few things worth trying:</p> <ul> <li><strong>Circle</strong>: Start at \(r = 0\) and increase slowly. Watch the \(H_1\) point (a loop) appear and persist. That persistent loop is the circle.</li> <li><strong>Two clusters</strong>: You’ll see two H₀ components born early, and then one dying when the clusters merge. The gap between birth and death tells you how far apart the clusters were.</li> <li><strong>Noisy circle vs. annulus</strong>: Compare the \(H_1\) signal in both. The annulus has a stronger, more persistent loop because the gap is more pronounced.</li> <li><strong>Figure-eight</strong>: Two loops appear in \(H_1\), then one dies when the shared region fills in. A figure-eight has the topology of a wedge of two circles.</li> </ul> <p>As you go through these examples, click on the various Cech and Vietoris-Rips labels on the legend. See if you can localize or identify where in the data each feature is coming from.</p> <h2 id="circle-of-circles">Circle of circles</h2> <p>One of the best features of persistent homology is its <strong>multiscale</strong> nature. Varying the radius \(r\) allows us to study several scales simultaneously without any practitioner input.</p> <p>Take for example a circle of circles. Imagine six circles arranged at the even hours on the face of a clock.</p> <p><img src="/assets/img/cir_of_cirs.png" alt="Six smaller circles arranged in a much larger circle." width="400" style="display:block; margin:auto"/></p> <p>What is the shape of this data? If you were “zoomed in”, you might only see the individual points but not their arrangement into circles. As you zoom out, you would start to see the 6 individual circles but still not their global arrangement into yet another circle. Finally as you zoom out more, the 6 individual circles might start to become difficult to make out, but you would definitely now start to see the global arrangement of a circle. Finally, as you zoom even farther out, you would see just a single lump of points.</p> <div style="display:flex; gap:1em; justify-content:center; margin:2em 0;"> <figure style="flex:1; text-align:center; margin:0;"> <img src="/assets/img/cir_of_cirs2.png" style="width:100%;"/> <figcaption style="font-size:0.8em; color:#888; margin-top:0.4em;">small r</figcaption> </figure> <figure style="flex:1; text-align:center; margin:0;"> <img src="/assets/img/cir_of_cirs3.png" style="width:100%;"/> <figcaption style="font-size:0.8em; color:#888; margin-top:0.4em;">medium r</figcaption> </figure> <figure style="flex:1; text-align:center; margin:0;"> <img src="/assets/img/cir_of_cirs4.png" style="width:100%;"/> <figcaption style="font-size:0.8em; color:#888; margin-top:0.4em;">large r</figcaption> </figure> </div> <p>Persistent homology is what makes this kind of “zooming out” analysis precise. All of the above information can be read off from the persistence diagram in a rigorous fashion.</p> <h2 id="vietorisrips-and-čech">Vietoris–Rips and Čech</h2> <p>The explorer shows two filtrations side by side: <strong>Vietoris–Rips</strong> and <strong>Čech</strong>. Both grow from the same point cloud but differ in when triangles are added.</p> <ul> <li>In Rips, a triangle is filled when the longest edge reaches \(2r\) — it’s purely combinatorial and fast to compute.</li> <li>In Čech, a triangle is filled when the three balls have a common intersection — geometrically precise but more expensive.</li> </ul> <p>Čech is theoretically tighter (it’s guaranteed to recover the correct topology by the nerve theorem), but Rips is close enough in practice and scales much better. For most applications, Rips is the right default.</p> <h2 id="what-the-diagram-is-really-saying">What the diagram is really saying</h2> <p>The persistence diagram is not just a scatter plot. It is a coordinate-free, multi-scale summary of shape. The stability theorem guarantees that small perturbations to the point cloud move points in the diagram by at most the same amount. This should be interpreted as saying the diagram is robust to noise in the data, an essential property for any real data science algorithm. But robustness is not the same as interpretability. The diagram records topology at every scale simultaneously, and which features matter depends on the question you are asking. Two point clouds sampled from the same geometric object will tend to produce similar diagrams, which is what makes them useful as features in downstream ML tasks. The utility here comes from the full multi-scale picture, not from filtering it or thresholding it arbitrarily down to the longest lived features.</p>]]></content><author><name></name></author><category term="math"/><category term="tda"/><category term="topology"/><category term="persistent-homology"/><summary type="html"><![CDATA[An interactive introduction to persistent homology — what it measures, how a filtration works, and what persistence diagrams are actually telling you.]]></summary></entry><entry><title type="html">TDA and Machine Learning</title><link href="https://catanzaromj.github.io/blog/2023/tda-and-ml/" rel="alternate" type="text/html" title="TDA and Machine Learning"/><published>2023-01-17T00:00:00+00:00</published><updated>2023-01-17T00:00:00+00:00</updated><id>https://catanzaromj.github.io/blog/2023/tda-and-ml</id><content type="html" xml:base="https://catanzaromj.github.io/blog/2023/tda-and-ml/"><![CDATA[<p>Machine learning is very good at finding patterns in data, but it is largely agnostic to <em>shape</em>. Two datasets can have the same statistical summary but wildly different geometry. Topological data analysis (TDA) is a collection of tools that make the shape of data precise and computable, which makes it a natural complement to standard ML pipelines.</p> <p>Shape is an intuitive concept to describe but difficult to quantify precisely. We first learn about shape in geometric terms, like circle, triangle, square, etc. But by the time we reach college, these seem like trivial concepts compared to derivatives and triple integrals of calculus. It turns out that understanding shape can have a great impact on how we interpret data. TDA is one such attempt to precisely quantify shape within data.</p> <h2 id="what-tda-offers">What TDA offers</h2> <p>TDA traditionally bifurcates into two types of applied tools with tons of theory interspersed between. The primary tool we’ll discuss here is <strong>persistent homology</strong><sup id="fnref:m"><a href="#fn:m" class="footnote" rel="footnote" role="doc-noteref">1</a></sup>. Given a dataset, like a point cloud, an image, or a time series, persistent homology tracks how topological features appear and disappear as a scale parameter varies. The topological features we find are things like connected components (think clusters), loops, voids, and higher dimensional analogues. The result is a succinct summary called a <strong>persistence diagram</strong>: a multiset of points in the plane where each point represents a feature, its birth scale on one axis, and its death scale on the other.</p> <p>There are a ton of really great introductions to TDA on the internet. I am not really attempting to do it any justice here. Here is a huge curated list of resources for anyone interested <a href="https://github.com/FatemehTarashi/awesome-tda">awesome-tda</a>.</p> <h2 id="bridging-tda-and-ml">Bridging TDA and ML</h2> <p>The issue is that persistence diagrams don’t sit in a vector space, so you can’t feed them directly into most ML models. Practically, this means you have to be very careful trying to define something like an “average” of persistence diagrams. It doesn’t work as you expect and oftentimes, you just can’t define an average. The same goes for lots of other statistical quantities we care about. Several representations or vectorization schemes have been developed to close this gap:</p> <ul> <li><strong>Persistence images</strong> bin the diagram into a 2D histogram, producing a fixed-size vector suitable for any standard classifier or neural network.</li> <li><strong>Persistence landscapes</strong> represent diagrams as a sequence of piecewise linear functions, which live in a Hilbert space and support averaging and kernels.</li> <li><strong>Topological layers</strong> (e.g., in <a href="https://giotto-ai.github.io/gtda-docs/latest/library.html"><code class="language-plaintext highlighter-rouge">giotto-tda</code></a> or <a href="https://pytorch-topological.readthedocs.io/en/latest/"><code class="language-plaintext highlighter-rouge">pytorch-topological</code></a>) compute persistent homology differentiably, letting you incorporate topological features end-to-end inside a neural network.</li> </ul> <p>Once you have a vector representation of your diagram, now you can continue down an ML pipeline. The outputs of these tools are compatible with ML models. This allows you to compute persistence diagrams, compute their persistence images, and then say, run a clustering algorithm on the collection of persistence images. In this way, the clustering is based on the topological information encoded in the diagram and represented by the image.</p> <h2 id="where-it-helps-in-practice">Where it helps in practice</h2> <p>TDA tends to add the most value in situations where local geometry matters:</p> <ul> <li><strong>Anomaly detection</strong> — topological features can flag unusual connectivity structure that density-based methods miss.</li> <li><strong>Graph and mesh data</strong> — persistent homology naturally handles irregular domains without requiring a fixed grid.</li> <li><strong>Time series</strong> — sliding-window embeddings turn a signal into a point cloud, and the resulting persistence diagram captures periodic and chaotic structure.</li> <li><strong>High-dimensional feature spaces</strong> — persistent homology can detect low-dimensional manifold structure even when ambient dimension is large.</li> </ul> <p>These are just a handful of examples and I hope to dive into some of these more in future posts.</p> <h2 id="getting-started">Getting started</h2> <p>One practical entry point for computing persistent homology is <a href="https://docs.scikit-tda.org/en/latest/"><code class="language-plaintext highlighter-rouge">scikit-tda</code></a>, which wraps several TDA libraries under a scikit-learn–compatible API. To be fair, I am currently a maintainer of scikit-tda so I am definitely biased. <a href="https://giotto-ai.github.io/gtda-docs/latest/library.html"><code class="language-plaintext highlighter-rouge">giotto-tda</code></a> is also a go-to TDA library with lots of slick improvements and parallelization built-in. Both of these libraries provide <code class="language-plaintext highlighter-rouge">Pipeline</code>-compatible transformers that slot directly into standard ML workflows. For differentiable TDA inside PyTorch, <code class="language-plaintext highlighter-rouge">pytorch-topological</code> and <a href="https://gudhi.inria.fr/"><code class="language-plaintext highlighter-rouge">gudhi</code></a>’s torch bindings are worth exploring.</p> <p>The best way to get started is to pick a dataset of interest and compute persistence using one of these libraries. There are various flavours of persistence, but newcomers should start with Vietoris-Rips persistence. It is generic enough to handle any type of data you throw at it, and is interpretable in low homological degrees.</p> <p>I don’t think that TDA is a replacement for standard ML, nor was it ever intended to be. I view TDA as describing another source of features and geometric intuition that standard methods lack. Used together, they cover more of the structure in your data than either approach alone.</p> <div class="footnotes" role="doc-endnotes"> <ol> <li id="fn:m"> <p>The other is the Mapper algorithm, which is also widely used in data science. <a href="#fnref:m" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> </ol> </div>]]></content><author><name></name></author><category term="math"/><category term="ml"/><category term="tda"/><category term="machine-learning"/><summary type="html"><![CDATA[A high-level, no code introduction to how topological data analysis and machine learning complement each other.]]></summary></entry></feed>